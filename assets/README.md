# Assets

Static, hand-authored teaching diagrams referenced by `docs/` and the
Streamlit GUI go here (e.g. a redrawn version of the paper's Fig. 1 block
diagram). Do not commit generated simulation output or figures produced by
`examples/` scripts — those belong in a student's own scratch directory.
